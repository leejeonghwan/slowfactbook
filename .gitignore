# Python
__pycache__/
*.pyc
.venv/
venv/

# Build output (regenerated by scripts/build.py + GitHub Actions)
site/

# Local extraction scratch
*.tmp
.DS_Store

# Keep source .key/.pptx OUT of git by default (use Git LFS instead if you
# want them versioned — see .gitattributes). Comment these lines to commit them.
source/*.key
source/*.pptx
